# describe-your-agent · V4

A supports click or drag onto an agent target with visible selected chips; B is a sequential capability interview, not a canvas. Both use exactly the 23 supplied primitives, create plain-words output, and export Markdown with ids and instructions for a model session. Selection is self-described, not measured. State stays within the page.

## Validation boundary

Plain HTML/CSS/JavaScript; no build required. Static checks cover local routes, form prohibition, manifest coverage, source data and script syntax. Browser visual QA is not claimed.
