# pricing · V4

A keeps the first column sticky and reports columns in view; B uses two button-selected columns; C expands features into all five cells. Desktop retains the complete table. Free stays first in A/C and the default B pair. All 14 rows and qualified cells are copied from comparison.json. The A4 landscape screen is print-ready. Prices remain 42px on cards at phone width, overriding the old mobile token.

## Validation boundary

Plain HTML/CSS/JavaScript; no build required. Static checks cover local routes, form prohibition, manifest coverage, source data and script syntax. Browser visual QA is not claimed.
