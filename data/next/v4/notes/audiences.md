# audiences · V4

A has five separate audience pages; B uses a persistent in-page switch; C places the lens only above the ladder. Every offer stays in the DOM and stays clickable. Leading and quiet cards use border/background hierarchy, never opacity or hiding. Copy comes from audiences.json. The supplied investor wording says both upper levels are signed; the specification table remains authoritative: level 03 is not signed off. We show the source wording only in the audience rationale, without altering comparison facts.

## Validation boundary

Plain HTML/CSS/JavaScript; no build required. Static checks cover local routes, form prohibition, manifest coverage, source data and script syntax. Browser visual QA is not claimed.

## Visual refinement

At the user’s request, audience doors now use a coordinated set of conceptual still-life images, simpler labels and shorter descriptions. The read-not-run chips were removed from these navigation cards. These are audience choices, not product-evidence claims. Other evidence chips remain. Mobile uses a swipeable row to keep the ABP hero close to the first screen.
