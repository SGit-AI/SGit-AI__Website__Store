# hero-headline · V4

Both requested headlines are drawn with the same Marketplace anatomy and linked trust strip. The main homepage retains the user’s approved Give your AI agent a clear mandate headline. ABP is named explicitly in every hero. Persona doors precede the product.

## Validation boundary

Plain HTML/CSS/JavaScript; no build required. Static checks cover local routes, form prohibition, manifest coverage, source data and script syntax. Browser visual QA is not claimed.
